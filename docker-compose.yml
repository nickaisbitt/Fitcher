# Local development services for Fitcher
# Usage: docker compose up -d
#
# Provides Postgres + Redis so the app doesn't need mock mode.
# The app itself runs via `npm start` on the host (not containerized)
# to make development easier.

services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: fitcher
      POSTGRES_PASSWORD: fitcher_dev
      POSTGRES_DB: fitcher
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fitcher"]
      interval: 5s
      timeout: 3s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  pgdata:
  redisdata:
