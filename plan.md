1. Modify `public/index.html` to add explanatory comments and document performance impact for `_calculateProfitFactor`.
2. Verify `public/index.html` changes using `read_file`.
3. Modify `public/index.html` to add explanatory comments and document performance impact for `bootstrapProfitFactorTest`.
4. Verify `public/index.html` changes using `read_file`.
5. Modify `public/index.html` to add explanatory comments and document performance impact for `_profitFactor`.
6. Verify `public/index.html` changes using `read_file`.
7. Modify `public/index.html` to add explanatory comments and document performance impact for `avgWin` and `avgLoss` calculations near line 3897.
8. Verify `public/index.html` changes using `read_file`.
9. Modify `src/services/metricsCollector.js` around lines 256-257 to add explanatory comments and document performance impact.
10. Verify `src/services/metricsCollector.js` changes using `read_file`.
11. Modify `src/services/backtestEngine.js` around lines 343-349 to add explanatory comments and document performance impact.
12. Verify `src/services/backtestEngine.js` changes using `read_file`.
13. Modify `src/services/backtestEngine.js` around lines 228-245 to replace the variance calculation loop with Welford's algorithm, add explanatory comments, and document the performance impact.
14. Verify `src/services/backtestEngine.js` changes using `read_file`.
15. Run the full test suite using `pnpm test` to ensure the changes are correct and have not introduced regressions.
16. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
17. Submit the change.
