--- public/index.html
+++ public/index.html
@@ -6532,7 +6537,10 @@
                     <div className="text-sm font-medium mb-3">📊 Position Exposure</div>
                     <div className="space-y-2">
                       {Object.keys(positions).length === 0 ? (
-                        <div className="text-slate-500 text-sm">No open positions</div>
+                        <div className="flex flex-col items-center justify-center text-slate-500 py-6">
+                          <div className="text-2xl mb-2 opacity-70">💼</div>
+                          <div className="text-sm font-medium text-slate-400">No active positions</div>
+                        </div>
                       ) : (
                         Object.entries(positions).map(([pair, pos]) => {
                           const currentPrice = prices[pair] || BASE_PRICES[pair];
@@ -6617,7 +6625,14 @@
               {activeTab === 'trades' && (
                 <div className="h-64 overflow-y-auto">
                   {trades.length === 0 ? (
-                    <div className="text-center text-slate-500 py-8">No trades yet</div>
+                    <div className="flex flex-col items-center justify-center text-slate-500 py-12">
+                      <div className="text-3xl mb-2 opacity-80">📊</div>
+                      <div className="font-medium text-slate-300">No trades yet</div>
+                      <div className="text-xs mt-1 mb-4 text-center max-w-[200px]">Your trading history will appear here once orders are executed.</div>
+                      {mode === 'paper' && (
+                        <button onClick={runBacktest} className="px-4 py-2 bg-slate-800 hover:bg-slate-700 rounded-lg text-sm transition border border-slate-700 focus-visible:ring-2 focus-visible:ring-cyan-500 focus:outline-none">Run Backtest</button>
+                      )}
+                    </div>
                   ) : (
                     <table className="w-full text-sm">
                       <thead className="text-slate-500 text-xs sticky top-0 bg-slate-800/90">
@@ -6743,7 +6758,12 @@
                       Close Position
                     </button>
                   </div>
                 ) : (
-                  <div className="text-slate-500 text-center py-6">No open position</div>
+                  <div className="flex flex-col items-center justify-center text-slate-500 py-8">
+                     <div className="text-3xl mb-2 opacity-70">🎯</div>
+                     <div className="font-medium text-slate-300">No open position</div>
+                     <div className="text-xs mt-1 mb-4 text-center">Select a pair to fetch data and look for opportunities.</div>
+                     <button onClick={() => fetchHistoricalData(selectedPair, timeframe)} className="px-4 py-2 bg-slate-800 hover:bg-slate-700 rounded-lg text-sm transition border border-slate-700 focus-visible:ring-2 focus-visible:ring-cyan-500 focus:outline-none">Fetch {selectedPair} Data</button>
+                  </div>
                 )}
               </div>
             </div>
